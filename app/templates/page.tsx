'use client';import {useEffect,useState} from 'react';import Link from 'next/link';
export default function Templates(){const [items,setItems]=useState<any[]>([]);const [f,setF]=useState({name:'',subject:'',body:''});async function load(){setItems(await (await fetch('/api/templates')).json())}useEffect(()=>{load()},[]);async function save(e:any){e.preventDefault();await fetch('/api/templates',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});setF({name:'',subject:'',body:''});load()}return <main className="wrap"><nav className="nav"><b>Templates</b><Link className="btn" href="/">Dashboard</Link></nav><div className="grid" style={{gridTemplateColumns:'1fr 1fr'}}><form className="card" onSubmit={save}><h2>New template</h2><label className="field">Name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label className="field">Subject<input required value={f.subject} onChange={e=>setF({...f,subject:e.target.value})}/></label><label className="field">Body<textarea required value={f.body} onChange={e=>setF({...f,body:e.target.value})}/></label><p className="muted">Use variables like {'{{first_name}}'} or {'{{company}}'}.</p><button className="btn primary">Save template</button></form><div className="card"><h2>Saved</h2>{items.map(t=><div key={t.id} style={{padding:'12px 0',borderBottom:'1px solid #eee'}}><b>{t.name}</b><div className="muted">{t.subject}</div></div>)}</div></div></main>}
